<% 
var params = templateParams
var patch = params.patch
var arr = params.image.src.split(",")
var src = patch + arr.shift();
var srcset = arr.join("," + patch);


%>
<div class="img">
    <img src="<%=src%>" srcset="<%=srcset%>"/>
</div>